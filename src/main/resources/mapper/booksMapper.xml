<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wyj.books.mapper.BooksMapper" >
    <resultMap id="BaseResultMap" type="com.wyj.books.entity.Books" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="picture" property="picture" jdbcType="VARCHAR" />
        <result column="classification" property="classification" jdbcType="VARCHAR" />
        <result column="totalnum" property="totalnum" jdbcType="INTEGER" />
        <result column="restnum" property="restnum" jdbcType="INTEGER" />
        <result column="location" property="location" jdbcType="VARCHAR" />
        <result column="author" property="author" jdbcType="VARCHAR" />
        <result column="publisher" property="publisher" jdbcType="VARCHAR" />
        <result column="publishdate" property="publishdate" jdbcType="DATE" />
        <result column="status" property="status" jdbcType="INTEGER" />
    </resultMap>
    <sql id="Base_Column_List" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, name, picture,classification,totalnum,restnum,location,author,publisher,publishdate,status
    </sql>
    <select id="selectAllBooks" resultType="com.wyj.books.entity.Books">
        SELECT * FROM books
    </select>

    <select id="getBookByClassification" resultType="com.wyj.books.entity.Books">
        SELECT * FROM books where classification = #{classification}
    </select>

    <select id="getClassification" resultType="java.lang.String">
        SELECT DISTINCT classification from books
    </select>

    <select id="selectBookById" resultType="com.wyj.books.entity.Books" parameterType="java.lang.Integer">
        SELECT * FROM books where id = #{id}
    </select>

    <select id="getByNameAndAuthor" resultType="com.wyj.books.entity.Books" parameterType="java.lang.String">
        select * from books where name = #{name} and author = #{author}
    </select>

    <!--模糊查询-->
    <select id="selectBooksLike" parameterType="String" resultType="com.wyj.books.entity.Books">
        select * from books
        <where>
            1 = 1
            <if test="searchStr !=null and searchStr !=''">
                and concat(id,name,picture,classification,location,author,publisher) LIKE concat ('%', #{searchStr},'%')
            </if>
        </where>
    </select>

    <insert id="addBook" parameterType="com.wyj.books.entity.Books" keyProperty="id" useGeneratedKeys="true">
        insert into books
        (name,picture,classification,totalnum,restnum,location,author,publisher,publishdate)
        values (#{name},#{picture},#{classification},#{totalnum},#{restnum},#{location},#{author},#{publisher},#{publishdate})
    </insert>

    <update id="updateBookById" parameterType="com.wyj.books.entity.Books">
        update books
        <set>
        <if test="name != null">
          name = #{name},
        </if>
        <if test="picture != null">
        picture = #{picture},
        </if>
        <if test="classification != null">
        classification = #{classification},
        </if>
        <if test="totalnum != null">
        totalnum = #{totalnum},
        </if>
        <if test="restnum != null">
        restnum = #{restnum},
        </if>
        <if test="status != null">
        status = #{status},
        </if>
        <if test="location != null">
        location = #{location},
        </if>
        <if test="author != null">
        author = #{author},
        </if>
        <if test="publisher != null">
        publisher = #{publisher},
        </if>
        <if test="publishdate != null">
        publishdate = #{publishdate},
        </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteBookById" parameterType="java.lang.Integer">
        delete FROM books where id = #{id}
    </delete>
</mapper>